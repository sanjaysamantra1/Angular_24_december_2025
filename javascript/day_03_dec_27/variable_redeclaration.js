var a = 10;
let b = 20;
const c = 30;
console.log(`a:${a} b:${b} c:${c}`);

var a = 100; // re-declaring 'a' varible
console.log(`a:${a} b:${b} c:${c}`);

let b = 200;   // cann't re-declare
const c = 300; // cann't re-declare
